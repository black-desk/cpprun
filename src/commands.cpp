#include "commands.hpp"

#include <cassert>
#include <iostream>

namespace cpprun
{

auto cmd_create(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

auto cmd_delete(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

auto cmd_exec(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

auto cmd_kill(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

auto cmd_list(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

auto cmd_start(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

auto cmd_state(std::map<std::string, docopt::value> & /*args*/) -> int
{
        return 0;
}

} // namespace cpprun
